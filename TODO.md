# DeskRealm TODO

## Active milestone — v0.6.0 performance

- Complete the Windows self-contained Release build.
- Keep the locally validated parallel destination-preparation path covered by one final log sample, then validate explicit mismatch compensation if a reproducible interruption can be produced; validate native virtual desktop switches separately.
- Validate exact realm-membership readiness on every managed realm.
- Validate topology variants and all lock modes, including current-variant-only manual overwrite on locked realms.
- Review final `[PERF]` logs, finish the release checklist, then publish one consolidated Git release.

## Later milestone — UI architecture

- Evaluate page-level `UserControl` decomposition and designer-assisted composition only after performance is stable.

See `TODO_v0.6.0-performance.md` for the complete implementation checklist.
