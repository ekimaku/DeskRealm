# Development history

This directory contains completed implementation TODOs retained as development history. They are not build instructions, source-repair templates or public release contracts.

The active release gate lives in the repository-root `TODO.md`. The public roadmap lives in `docs/ROADMAP.md`.
