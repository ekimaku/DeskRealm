# DeskRealm v0.7.0 — Direct controls static validation (`_be`)

## Completed in this environment

- Parsed every `.xaml`, `.csproj` and application manifest XML file.
- Checked C# lexical delimiter balance after the direct-control implementation.
- Verified the direct-control routes from VCards to `DeskRealmRuntime` and `DesktopSwitchService`.
- Verified wallpaper reconciliation, quick wallpaper/hotkey actions, default-star actions, inherited-lock snapshot fields and controlled restart service are present.
- Checked for French UI text introduced by this milestone; the new user-facing strings are English.
- Verified `VERSION.txt` and the project assembly version remain `0.7.0`.
- Verified no generated output is included before packaging.

## Not possible in this environment

This environment does not provide Windows, `dotnet`, PowerShell, Windows App SDK tooling, Explorer, Task View or a notification area. It cannot perform WinUI compilation, self-contained publish, wallpaper Registry integration, taskbar/tray recovery or icon-layout smoke validation.

## Required Windows gate

Run `./scripts/Build-Release.ps1` and then complete the direct-control section in `SMOKE_TEST.md` from a clean extracted candidate folder.
