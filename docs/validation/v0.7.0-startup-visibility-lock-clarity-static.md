# Static validation — v0.7.0 `_bi`

## Verified in source

- `RealmConfig.CurrentVersion` is 18 and `startMinimized` defaults to `true`.
- `DesktopSwitchService` applies the v18 migration after earlier runtime migrations.
- `DeskRealmLaunchState`, `RealmStudioSnapshot`, `MainViewModel`, `MainWindow` and `App` all carry and apply the startup visibility setting.
- First-run import is explicitly activated before its modal path; minimized startup is used only for normal launches.
- Automation saves `startMinimized` alongside existing global settings.
- Realm lock glyphs report current state and tooltips describe the next toggle action.

## Environment limit

This environment does not include Windows, PowerShell, the .NET Windows SDK or Windows App SDK tooling. The actual WinUI build, publish and Shell behavior must be validated through `scripts/Build-Release.ps1` on Windows.
