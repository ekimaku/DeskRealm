eskRealm v0.6.0 — release-control validation

Date: 2026-06-19
Local release-control archive: `DeskRealm_v0_6_0_ah.zip`
Application version: `0.6.0`

## Purpose

This pass is a release-control and documentation polish pass for the public v0.6.0 package. It does not change runtime behavior. The goal is to make sure reviewers can understand what v0.6.0 improves, what has already been validated, and what should be checked before attaching release binaries.

## Source/package checks

- `VERSION.txt`, project metadata, README, CHANGELOG, release notes and release checklist remain aligned on `0.6.0`.
- Local archive suffix `_ag` is used only in the ZIP filename and does not appear in compiled/application version metadata.
- README now includes a plain-English performance-gains table.
- CHANGELOG and release notes include the startup recovery, parallel hotkey pipeline, persistent worker, exact realm readiness and current-variant-only manual save safeguards.
- `SMOKE_TEST.md` separates already validated local runtime checks from remaining release blockers.
- No `bin`, `obj`, `dist`, `.release-work`, `.git` directory or nested source ZIP is included in the final source archive.

## Runtime behavior already validated locally

- Debug source run starts successfully through `scripts\Run-DeskRealm.ps1`.
- Startup existing-realm recovery restores the saved layout instead of accepting `Already on...` as sufficient proof.
- The persistent worker starts and restores the active realm layout.
- Parallel hotkey switching works smoothly in local testing.

## Remaining release blockers

- Run `scripts\Build-Release.ps1` on Windows.
- Finish the complete `SMOKE_TEST.md` checklist.
- Keep one final log sample showing the parallel hotkey transaction branches and final GUID barrier.
- Smoke-test the final GitHub release assets after publication.

## Release decision

Publish one consolidated `v0.6.0` release; do not publish internal candidate suffixes. Before attaching compiled binaries, run the Windows Release build and final smoke checklist on the release package.
